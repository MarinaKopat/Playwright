name: Playwright Tests (Python)
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11' # Укажите свою версию Python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Установка библиотек из вашего файла requirements.txt
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        # Дополнительная установка самого фреймворка, если его нет в requirements.txt
        pip install pytest-playwright

    - name: Install Playwright Browsers
      run: playwright install --with-deps

    - name: Run Pytest tests
      run: pytest

    - name: Upload Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report
        path: allure-results/ # Если используете Allure, или укажите путь к вашим отчетам
        retention-days: 30
